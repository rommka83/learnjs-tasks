elem.append(document.createTextNode(text)); // создание текстового узла и добавление текста
elem.innerHTML = text; // добавление HTML с текстом
elem.textContent = text; // создание текстового узла и добавление текста
